{
  "name": "id-pain-gong-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Vercel Functions wrapper for Gong API (replaces the legacy MCP HTTP server)",
  "scripts": {
    "dev": "vercel dev",
    "build": "node scripts/ensure-public.mjs"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
